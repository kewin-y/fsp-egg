// This is an example Egg program

// The pen starts at (0, 0), so we can start moving it:
// This moves the pen 50 steps up, drawing a black line
pen_move 50

// This changes the pen's color to blue
pen_color BLUE

// Rotate the pen to draw cool shapes!
// This rotates the pen 90 degrees clockwise
pen_rotate 90

// Pick the pen up 
pen_up

pen_move 
